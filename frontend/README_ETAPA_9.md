# Frontend — Sprint 2 / Etapa 9

A rota `/settings` agora possui uma interface administrativa completa.

## Seções

- Empresa
- Operação comercial
- Preferências
- Integrações

## APIs reutilizadas

- `GET /api/v1/segments`
- `GET /api/v1/workspaces/{id}/segment-config`
- `PUT /api/v1/workspaces/{id}/segment-config`
- `PATCH /api/v1/workspaces/{id}`

Não há novas dependências npm nesta etapa.

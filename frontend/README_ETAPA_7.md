# Sprint 2 / Etapa 7 — Frontend de Relatórios

A rota `/reports` agora usa dados reais da API do Nexyra CRM.

## Validação

```powershell
cd C:\Nexyra_CRM\frontend
npm run build
npm run dev
```

Abra `http://127.0.0.1:5173` e acesse **Relatórios**.
